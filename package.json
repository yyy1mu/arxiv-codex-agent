{
  "name": "arxiv-security-agent",
  "version": "1.0.0",
  "description": "Daily arXiv security paper fetch → review → score agent (Bun + TS)",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "today": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.142.5",
    "fast-xml-parser": "^4.5.0",
    "puppeteer-core": "^25.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
